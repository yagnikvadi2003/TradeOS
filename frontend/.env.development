# Development-only defaults (committed; contains no secrets).
# Simulated market data so the terminal renders without the backend.
VITE_MARKET_DATA_SOURCE=mock
VITE_PUBLIC_ORIGIN=http://localhost:5173
